Check for updates:

```
:lua vim.pack.update()
```

This opens a readonly buffer that shows the changes in the installed plugins.

`:w` will apply the changes and update the plugins, `:q` will discard the updates.


