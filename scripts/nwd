#!/bin/bash
"$@"
exit_code=$?
if [ $exit_code -eq 0 ]; then
    notify-send "Command Completed" "✅ '$*' finished successfully"
else
    notify-send "Command Failed" "❌ '$*' failed with exit code $exit_code"
fi
exit $exit_code

